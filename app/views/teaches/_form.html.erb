<%= form_with(model: teach) do |form| %>
  <% if teach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teach.errors.count, "error") %> prohibited this teach from being saved:</h2>
      <ul>
        <% teach.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id %>
  </div>
  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
