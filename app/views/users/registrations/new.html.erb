<!--<h2>Sign up</h2>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "users/shared/error_messages", resource: resource %>
  <div class="field">
    <%= f.file_field :avatar %>
  </div>
  <div class="field">
    <%= f.label :email %><br/>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>
  <div class="field">
    <%= f.label :nome %><br/>
    <%= f.text_field :nome, autofocus: true, autocomplete: "nome" %>
  </div>
  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %>
        characters minimum)</em>
    <% end %><br/>
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>
  <div class="field">
    <%= f.label :password_confirmation %><br/>
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>
  <div class="field">
    <%= f.select :ruolo, ['Studente', 'Insegnante']%>
  </div>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>
-->
<section class="vh-100">
  <div class="container-fluid h-custom">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class=" col-md-6 col-xl-5 p-3 d-none d-md-block ">
        <%= image_tag("classroom.png", class: "img-fluid") %>
      </div>
      <div class=" col-md-6 col-xl-4 offset-xl-1">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= render "users/shared/error_messages", resource: resource %>
          <div class="d-flex flex-row align-items-center justify-content-center justify-content-lg-center">
            <p class="lead fw-normal mb-0 me-3">Registrati </p>
          </div>
          <div class="divider d-flex align-items-center my-4">
            <p class="text-center fw-bold mx-3 mb-0"></p>
          </div>
          <!-- Email input -->
          <div class="form-outline mb-2">
            <%= f.email_field :email, autofocus: true, autocomplete: "new-email", class: "form-control form-control-lg", placeholder: "Email" %>
          </div>
          <!-- Password input -->
          <div class="form-outline mb-2">
            <%= f.password_field :password, autocomplete: "new-password", placeholder: "Password (minimo 6 caratteri)", class: "form-control form-control-lg"  %>
          </div>
          <div class="form-outline mb-2">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "Conferma la password", class: "form-control form-control-lg"  %>
          </div>
          <div class="divider d-flex align-items-center my-3">
            <p class="text-center fw-bold mx-3 mb-0"></p>
          </div>
          <div class="mb-2">
            <label  class="form-label">Carica una tua foto (facoltativo)</label>
            <%= f.file_field :avatar, class: "form-control" %>
          </div>
          <div class="mb-2">
            <%= f.text_field :nome, autofocus: true, autocomplete: "nome", class: "form-control form-control-lg", placeholder: "Nome e Cognome" %>
          </div>
          <div class="mb-2">
            <%= f.select :ruolo, options_for_select([
  ["Studente o Insegnante?", ""], 
  "Studente", "Insegnante"]), 
  {}, { class: "form-select pt-2 mt-0" } %>
          </div>
          <div class="divider d-flex align-items-center my-4">
            <p class="text-center fw-bold mx-3 mb-0">Solo per insegnanti</p>
          </div>
          <div class="input-group mb-2">
            <span class="input-group-text ">Descriviti brevemente</span>
            <%= f.text_area :descrizione, class: "form-control", rows: "2"%>
          </div>
          <div class="text-center text-lg-center mt-0 pt-2">
            <%= f.submit "Crea", class: "btn darkR  btn-lg", style: "padding-left: 2.5rem; padding-right: 2.5rem;"%>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>
